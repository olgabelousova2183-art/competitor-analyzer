# Какой API выбрать: Proxy API или OpenAI API?

## Если у вас есть оба ключа

### 🔄 Как работает система:

**Текущая логика выбора:**
- ✅ Если указаны `PROXY_API_KEY` и `PROXY_API_URL` → **используется Proxy API** (приоритет)
- ✅ Если указан только `OPENAI_API_KEY` → используется прямой OpenAI API

## Рекомендации:

### Используйте Proxy API, если:
- ✅ Вы находитесь в стране, где OpenAI заблокирован
- ✅ Вам нужна дополнительная стабильность соединения
- ✅ Провайдер Proxy API предлагает лучшие тарифы
- ✅ Нужна дополнительная безопасность/анонимность

### Используйте прямой OpenAI API, если:
- ✅ OpenAI доступен напрямую в вашей стране
- ✅ Хотите максимальную скорость (меньше посредников)
- ✅ Хотите работать напрямую с OpenAI
- ✅ У вас хорошая скорость интернета

## Настройка файла .env

### Вариант 1: Использовать Proxy API (рекомендуется, если оба есть)

Создайте файл `.env` с содержимым:
```env
# Proxy API Configuration
PROXY_API_KEY=ваш_proxy_ключ
PROXY_API_URL=https://api.ваш-прокси-сервис.com/v1
```

**НЕ добавляйте** `OPENAI_API_KEY` в этом случае!

### Вариант 2: Использовать прямой OpenAI API

Создайте файл `.env` с содержимым:
```env
# OpenAI API Configuration
OPENAI_API_KEY=sk-ваш_openai_ключ
```

**НЕ добавляйте** `PROXY_API_KEY` и `PROXY_API_URL` в этом случае!

## Пример правильной настройки

### ✅ Правильно (используется Proxy API):
```env
PROXY_API_KEY=abc123xyz789
PROXY_API_URL=https://api.proxy-service.com/v1
```

### ✅ Правильно (используется OpenAI API):
```env
OPENAI_API_KEY=sk-proj-abc123xyz789
```

### ❌ НЕ правильно (может вызвать путаницу):
```env
OPENAI_API_KEY=sk-abc123
PROXY_API_KEY=xyz789
PROXY_API_URL=https://api.proxy.com/v1
```
В этом случае будет использоваться Proxy API (он имеет приоритет), но это может вызвать путаницу.

## Моя рекомендация для вас:

**Если у вас оба ключа, я рекомендую:**

1. **Начните с Proxy API**, если:
   - У вас есть проблемы с доступом к OpenAI
   - Proxy API работает стабильнее

2. **Используйте OpenAI API**, если:
   - У вас нет проблем с доступом
   - Хотите максимальную скорость
   - Работаете в стране, где OpenAI не заблокирован

**Можно попробовать оба и выбрать тот, который работает лучше!**

## Быстрая настройка:

Используйте скрипт для автоматической настройки:
```bash
python create_env.py
```

Выберите:
- `1` для OpenAI API
- `2` для Proxy API

