# 📖 Пошаговая инструкция: Где и как запускать проект

## Где запускать команды?

**Все команды нужно запускать в терминале/командной строке/PowerShell**

---

## 🚀 Способ 1: Через PowerShell (рекомендуется для Windows)

### Шаг 1: Откройте PowerShell в папке проекта

**Вариант А: Через Проводник**
1. Откройте папку проекта: `C:\Users\user\Desktop\дз промпт 15.12`
2. В адресной строке проводника введите: `powershell` и нажмите Enter
   - ИЛИ нажмите `Shift + Правый клик` в пустом месте папки
   - Выберите "Открыть окно PowerShell здесь"

**Вариант Б: Через меню Пуск**
1. Нажмите `Win + X` или кликните правой кнопкой на меню Пуск
2. Выберите "Windows PowerShell" или "Терминал"
3. Перейдите в папку проекта:
   ```powershell
   cd "C:\Users\user\Desktop\дз промпт 15.12"
   ```

### Шаг 2: Убедитесь, что вы в правильной папке

Выполните команду:
```powershell
pwd
```

Должно показать: `C:\Users\user\Desktop\дз промпт 15.12`

Если нет, перейдите в папку:
```powershell
cd "C:\Users\user\Desktop\дз промпт 15.12"
```

---

## 🚀 Способ 2: Через командную строку (CMD)

1. Нажмите `Win + R`
2. Введите `cmd` и нажмите Enter
3. Перейдите в папку проекта:
   ```cmd
   cd "C:\Users\user\Desktop\дз промпт 15.12"
   ```

---

## 🚀 Способ 3: Через VS Code / PyCharm

1. Откройте папку проекта в редакторе
2. Откройте встроенный терминал:
   - VS Code: `Ctrl + ` (Ctrl + обратная кавычка)
   - PyCharm: View → Tool Windows → Terminal
3. Терминал автоматически откроется в папке проекта

---

## 📝 Пошаговый запуск проекта

### Шаг 1: Установите зависимости (если еще не установили)

В терминале выполните:
```bash
pip install -r requirements.txt
```

**Где выполнять:** В PowerShell/CMD в папке проекта

---

### Шаг 2: Запустите API сервер

В терминале выполните:
```bash
python run.py
```

**Где выполнять:** В PowerShell/CMD в папке проекта

**Что вы увидите:**
```
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
INFO:     Started reloader process
INFO:     Started server process
INFO:     Waiting for application startup.
INFO:     Application startup complete.
```

**✅ Это значит сервер запущен!**

**⚠️ Важно:** Не закрывайте это окно терминала! Сервер должен работать постоянно.

---

### Шаг 3: Откройте новый терминал для Desktop приложения

**Нужно открыть ВТОРОЙ терминал** (первый должен оставаться с запущенным сервером)

1. Снова откройте PowerShell/CMD в папке проекта
   - Или нажмите `Ctrl + Shift + ` в VS Code для нового терминала

2. В новом терминале выполните:
   ```bash
   python run_desktop.py
   ```

**Что вы увидите:**
- Откроется окно приложения "Competitor Analyzer"
- В нем будут вкладки: Image Analysis, Text Analysis, Parsing, History

---

## 🎯 Визуальная схема:

```
┌─────────────────────────────────────────┐
│  ТЕРМИНАЛ 1 (PowerShell/CMD)            │
│  cd "C:\Users\user\Desktop\дз промпт 15.12" │
│  python run.py                           │
│  ↓                                       │
│  [Сервер работает на http://localhost:8000]│
│  (НЕ ЗАКРЫВАТЬ!)                         │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  ТЕРМИНАЛ 2 (PowerShell/CMD)            │
│  cd "C:\Users\user\Desktop\дз промпт 15.12" │
│  python run_desktop.py                   │
│  ↓                                       │
│  [Откроется окно Desktop приложения]     │
└─────────────────────────────────────────┘
```

---

## 🌐 Альтернатива: Использовать только API через браузер

Если не хотите запускать Desktop приложение, можно работать напрямую с API:

1. Запустите сервер (как в Шаге 2)
2. Откройте браузер и перейдите на:
   - http://localhost:8000 - главная страница
   - http://localhost:8000/docs - интерактивная документация API (Swagger UI)

В Swagger UI можно протестировать все эндпоинты прямо из браузера!

---

## ⚠️ Частые проблемы и решения

### Проблема: "python не является внутренней или внешней командой"

**Решение:** Установите Python и добавьте его в PATH, или используйте:
```bash
py run.py
```
вместо `python run.py`

### Проблема: "ModuleNotFoundError"

**Решение:** Установите зависимости:
```bash
pip install -r requirements.txt
```

### Проблема: Порт 8000 уже занят

**Решение:** Измените порт в файле `config.py`:
```python
API_PORT = 8001  # или другой свободный порт
```

### Проблема: Не могу найти папку проекта

**Решение:** 
1. Откройте Проводник
2. Перейдите в `C:\Users\user\Desktop\дз промпт 15.12`
3. В адресной строке введите `powershell` и нажмите Enter

---

## 📋 Чек-лист перед запуском:

- [ ] Python установлен (проверьте: `python --version`)
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] Файл `.env` создан с API ключом
- [ ] Открыт терминал в папке проекта
- [ ] Вы в правильной папке (проверьте: `pwd` или `cd`)

---

## ✅ Готово к работе!

После выполнения всех шагов у вас будет:
1. ✅ API сервер работает на http://localhost:8000
2. ✅ Desktop приложение открыто и готово к использованию

Удачи! 🎉

