# Пошаговая инструкция: Как добавить API ключи

## Способ 1: Через текстовый редактор (рекомендуется)

### Шаг 1: Откройте папку проекта
Перейдите в папку: `C:\Users\user\Desktop\дз промпт 15.12`

### Шаг 2: Создайте файл .env
1. Кликните правой кнопкой мыши в пустом месте папки
2. Выберите "Создать" → "Текстовый документ"
3. Назовите файл точно так: `.env` (точка в начале важна!)
   - Если Windows не дает создать файл с точкой в начале:
     - Создайте файл с именем `env` (без точки)
     - Переименуйте его в `.env.` (с точкой в конце)
     - Windows автоматически уберет последнюю точку

### Шаг 3: Откройте файл .env в блокноте
1. Кликните правой кнопкой на файле `.env`
2. Выберите "Открыть с помощью" → "Блокнот"

### Шаг 4: Вставьте ваш API ключ

#### Вариант A: Если у вас OpenAI API ключ
Скопируйте и вставьте в файл следующее (замените `your_openai_api_key_here` на ваш реальный ключ):

```
OPENAI_API_KEY=sk-your-actual-api-key-here
```

**Пример:**
```
OPENAI_API_KEY=sk-proj-abc123xyz789...
```

#### Вариант B: Если используете Proxy API
Если вы используете прокси-сервис для OpenAI API:

```
PROXY_API_KEY=your_proxy_api_key_here
PROXY_API_URL=https://api.proxy.com/v1
```

### Шаг 5: Сохраните файл
Нажмите `Ctrl + S` или "Файл" → "Сохранить"

### Шаг 6: Убедитесь, что файл находится в правильной папке
Файл `.env` должен находиться в корне проекта, там же где файлы:
- `main.py`
- `config.py`
- `requirements.txt`
- `README.md`

---

## Способ 2: Через командную строку (PowerShell)

### Шаг 1: Откройте PowerShell в папке проекта
1. Откройте папку проекта в проводнике
2. Нажмите `Shift + Правый клик` в пустом месте папки
3. Выберите "Открыть окно PowerShell здесь"

### Шаг 2: Создайте файл .env
Выполните команду:

```powershell
notepad .env
```

### Шаг 3: Вставьте ваш API ключ
В открывшемся блокноте вставьте:

```
OPENAI_API_KEY=ваш_ключ_здесь
```

### Шаг 4: Сохраните и закройте
Нажмите `Ctrl + S` и закройте блокнот

---

## Способ 3: Через Python скрипт (автоматический)

Я могу создать файл `.env` автоматически, если вы предоставите API ключ.

---

## Где взять OpenAI API ключ?

### Если у вас еще нет ключа:

1. **Перейдите на сайт OpenAI:**
   - Откройте браузер
   - Перейдите на https://platform.openai.com/

2. **Зарегистрируйтесь или войдите:**
   - Если у вас нет аккаунта - зарегистрируйтесь
   - Если есть - войдите в систему

3. **Создайте API ключ:**
   - Перейдите в раздел "API Keys" (обычно в правом верхнем углу)
   - Нажмите "Create new secret key"
   - Скопируйте созданный ключ (он показывается только один раз!)
   - Ключ начинается с `sk-`

4. **Вставьте ключ в файл .env**

---

## Проверка правильности настройки

### Проверка 1: Файл существует
Убедитесь, что файл `.env` существует в корне проекта

### Проверка 2: Формат правильный
Файл должен содержать только одну строку (или две, если используете Proxy):
```
OPENAI_API_KEY=sk-ваш-ключ-здесь
```

**ВАЖНО:**
- ❌ НЕ добавляйте пробелы вокруг знака `=`
- ❌ НЕ добавляйте кавычки вокруг ключа
- ✅ Правильно: `OPENAI_API_KEY=sk-abc123`
- ❌ Неправильно: `OPENAI_API_KEY = "sk-abc123"`

### Проверка 3: Запустите тест
Попробуйте запустить API сервер:

```bash
python run.py
```

Если все правильно, сервер запустится без ошибок.

---

## Безопасность

⚠️ **ВАЖНО:**
- ✅ Файл `.env` уже добавлен в `.gitignore` - он НЕ будет загружен в репозиторий
- ✅ НЕ делитесь файлом `.env` с другими
- ✅ НЕ публикуйте ваш API ключ в коде или на GitHub
- ✅ НЕ коммитьте файл `.env` в git

---

## Примеры правильного содержимого файла .env

### Пример 1: OpenAI API ключ
```
OPENAI_API_KEY=sk-proj-abc123def456ghi789jkl012mno345pqr678stu901vwx234yz
```

### Пример 2: Proxy API
```
PROXY_API_KEY=proxy_key_12345
PROXY_API_URL=https://api.proxy-service.com/v1
```

---

## Проблемы и решения

### Проблема: Windows не показывает файл .env
**Решение:** В проводнике включите показ скрытых файлов:
1. Вид → Показать → Скрытые элементы

### Проблема: Не могу создать файл с точкой в начале
**Решение:** Используйте командную строку:
```powershell
New-Item -Path .env -ItemType File
```

### Проблема: API ключ не работает
**Решение:** 
- Проверьте, что ключ скопирован полностью
- Убедитесь, что нет пробелов в начале или конце
- Проверьте, что у вас есть доступ к OpenAI API (достаточно средств на счету)

---

## Готово! 🎉

После создания файла `.env` с вашим API ключом, проект готов к использованию!

