# 🚀 Быстрая публикация на GitHub

## Пошаговая инструкция (5 минут)

### Шаг 1: Установите Git (если еще не установлен)

Скачайте с https://git-scm.com/download/win и установите.
Перезапустите PowerShell после установки.

---

### Шаг 2: Запустите скрипт публикации

В PowerShell в папке проекта выполните:

```powershell
.\git_publish.ps1
```

Скрипт автоматически:
- ✅ Проверит наличие Git
- ✅ Проверит, что `.env` исключен из репозитория
- ✅ Инициализирует Git репозиторий
- ✅ Добавит все файлы
- ✅ Создаст коммит

---

### Шаг 3: Создайте репозиторий на GitHub

1. Перейдите на https://github.com/new
2. **Repository name:** `competitor-analyzer` (или другое имя)
3. **Description:** `AI-powered competitor market analyzer with image/text analysis and web scraping`
4. Выберите **Public** (публичный)
5. **НЕ** отмечайте "Initialize with README" (у нас уже есть)
6. Нажмите **"Create repository"**

---

### Шаг 4: Подключите и опубликуйте

GitHub покажет инструкции. Выполните (замените `YOUR_USERNAME`):

```powershell
git remote add origin https://github.com/YOUR_USERNAME/competitor-analyzer.git
git push -u origin main
```

Готово! 🎉

---

## Альтернатива: Ручная публикация

Если не хотите использовать скрипт, выполните команды вручную:

```powershell
# 1. Инициализация
git init

# 2. Добавление файлов
git add .

# 3. Проверка (убедитесь, что .env НЕ в списке!)
git status

# 4. Коммит
git commit -m "Initial commit: AI Competitor Market Analyzer"

# 5. Переименование ветки
git branch -M main

# 6. Подключение к GitHub (замените YOUR_USERNAME)
git remote add origin https://github.com/YOUR_USERNAME/competitor-analyzer.git

# 7. Публикация
git push -u origin main
```

---

## ⚠️ Проверка безопасности

Перед публикацией убедитесь:

1. **Файл `.env` не в репозитории:**
   ```powershell
   git ls-files | Select-String ".env"
   ```
   Если ничего не выводится - хорошо!

2. **`.gitignore` содержит `.env`:**
   Откройте `.gitignore` и проверьте, что там есть строка `.env`

3. **Нет API ключей в коде:**
   Проверьте файлы на наличие `sk-`:
   ```powershell
   Select-String -Path *.py -Pattern "sk-[a-zA-Z0-9]{20,}" -SimpleMatch
   ```

---

## 📝 После публикации

1. Добавьте описание проекта на странице репозитория
2. Добавьте теги (tags) если нужно
3. Проверьте, что README отображается правильно
4. Поделитесь ссылкой на репозиторий!

---

**Подробная инструкция:** см. файл `ПУБЛИКАЦИЯ_НА_GITHUB.md`

