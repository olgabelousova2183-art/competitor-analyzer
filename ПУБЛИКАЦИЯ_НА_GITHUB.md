# 🚀 Публикация проекта на GitHub

## Подготовка проекта

✅ **Проверено:**
- Файл `.gitignore` правильно настроен (`.env` уже исключен)
- `README.md` содержит описание проекта
- Все конфиденциальные данные в `.env` (который не будет загружен)

---

## Шаг 1: Установка Git (если еще не установлен)

### Вариант 1: Установка через официальный сайт

1. Перейдите на https://git-scm.com/download/win
2. Скачайте установщик для Windows
3. Запустите установщик и следуйте инструкциям
4. Используйте настройки по умолчанию (они оптимальны)

### Вариант 2: Установка через Winget (если доступен)

```powershell
winget install --id Git.Git -e --source winget
```

### Проверка установки

После установки закройте и откройте PowerShell заново, затем выполните:

```powershell
git --version
```

Должно показать версию Git (например, `git version 2.42.0`)

---

## Шаг 2: Настройка Git (первый раз)

Если вы используете Git впервые, настройте имя и email:

```powershell
git config --global user.name "Ваше Имя"
git config --global user.email "ваш.email@example.com"
```

---

## Шаг 3: Инициализация репозитория

Откройте PowerShell в папке проекта и выполните:

```powershell
cd "C:\Users\user\Desktop\дз промпт 15.12"
git init
```

---

## Шаг 4: Добавление файлов в Git

Добавьте все файлы проекта (`.env` автоматически будет проигнорирован благодаря `.gitignore`):

```powershell
git add .
```

Проверьте, что `.env` НЕ добавлен:

```powershell
git status
```

Вы НЕ должны видеть `.env` в списке добавленных файлов!

---

## Шаг 5: Создание первого коммита

```powershell
git commit -m "Initial commit: AI Competitor Market Analyzer"
```

---

## Шаг 6: Создание репозитория на GitHub

### Через веб-интерфейс:

1. Перейдите на https://github.com
2. Войдите в свой аккаунт (или создайте новый)
3. Нажмите кнопку **"+"** в правом верхнем углу
4. Выберите **"New repository"**
5. Заполните форму:
   - **Repository name:** `competitor-analyzer` (или другое имя)
   - **Description:** `AI-powered competitor market analyzer with image/text analysis and web scraping`
   - **Visibility:** Выберите **Public** (чтобы можно было просмотреть решение)
   - **НЕ** отмечайте "Initialize this repository with a README" (у нас уже есть README)
6. Нажмите **"Create repository"**

### Через GitHub CLI (если установлен):

```powershell
gh repo create competitor-analyzer --public --description "AI-powered competitor market analyzer"
```

---

## Шаг 7: Подключение к GitHub и публикация

GitHub покажет инструкции. Выполните команды (замените `YOUR_USERNAME` на ваш GitHub username):

```powershell
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/competitor-analyzer.git
git push -u origin main
```

**Пример:**
Если ваш username `john_doe`, команда будет:
```powershell
git remote add origin https://github.com/john_doe/competitor-analyzer.git
```

---

## Шаг 8: Проверка

После выполнения `git push`:
1. Обновите страницу репозитория на GitHub
2. Убедитесь, что все файлы загружены
3. Проверьте, что файла `.env` **НЕТ** в репозитории

---

## ⚠️ Важные проверки перед публикацией

### Проверка 1: Убедитесь, что .env не в Git

```powershell
git ls-files | Select-String ".env"
```

Если команда ничего не выводит - всё хорошо! `.env` не в репозитории.

### Проверка 2: Проверьте .gitignore

Откройте файл `.gitignore` и убедитесь, что там есть строка:
```
.env
```

✅ Она уже есть!

### Проверка 3: Проверьте, что нет конфиденциальных данных в коде

Проверьте файлы на наличие API ключей:

```powershell
Select-String -Path *.py -Pattern "sk-[a-zA-Z0-9]" -SimpleMatch
```

Если ничего не найдено - отлично!

---

## 📋 Быстрая последовательность команд (после установки Git):

```powershell
# 1. Перейдите в папку проекта
cd "C:\Users\user\Desktop\дз промпт 15.12"

# 2. Инициализируйте репозиторий
git init

# 3. Добавьте файлы
git add .

# 4. Проверьте статус (убедитесь, что .env нет в списке)
git status

# 5. Создайте коммит
git commit -m "Initial commit: AI Competitor Market Analyzer"

# 6. Переименуйте ветку в main
git branch -M main

# 7. Добавьте удаленный репозиторий (замените YOUR_USERNAME)
git remote add origin https://github.com/YOUR_USERNAME/competitor-analyzer.git

# 8. Опубликуйте на GitHub
git push -u origin main
```

---

## 🔒 Безопасность

✅ **Что НЕ будет в репозитории:**
- `.env` - файл с API ключами
- `history/` - папка с историей анализов
- `__pycache__/` - кеш Python
- `dist/`, `build/` - временные файлы сборки

✅ **Что БУДЕТ в репозитории:**
- Весь исходный код
- `README.md` с инструкциями
- `requirements.txt` с зависимостями
- `.gitignore` для защиты конфиденциальных данных
- Все файлы конфигурации (кроме `.env`)

---

## 📝 После публикации

1. Скопируйте URL вашего репозитория
2. Добавьте описание проекта на странице репозитория
3. Можно добавить теги (tags) для категоризации
4. Добавьте ссылку на репозиторий в README, если нужно

---

## 🆘 Если что-то пошло не так

### Проблема: Git не найден после установки
**Решение:** Перезапустите PowerShell или перезагрузите компьютер

### Проблема: Ошибка аутентификации при push
**Решение:** Используйте Personal Access Token вместо пароля:
1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Создайте новый token с правами `repo`
3. Используйте token как пароль при push

### Проблема: .env случайно добавлен в репозиторий
**Решение:** Если это произошло, немедленно:
```powershell
git rm --cached .env
git commit -m "Remove .env from repository"
git push
```
И затем измените ваш API ключ на новый!

---

**Удачи с публикацией! 🎉**

