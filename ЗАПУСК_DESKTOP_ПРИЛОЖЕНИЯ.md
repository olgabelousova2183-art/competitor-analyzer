# Как запустить Desktop приложение

## ⚠️ Важно: Сначала перейдите в папку проекта!

### Шаг 1: Перейдите в папку проекта

В PowerShell выполните:

```powershell
cd "C:\Users\user\Desktop\дз промпт 15.12"
```

Или просто:
```powershell
cd Desktop\дз` промпт` 15.12
```

(Обратите внимание на обратные кавычки вокруг пробелов, или используйте кавычки как в первом варианте)

### Шаг 2: Убедитесь, что вы в правильной папке

Проверьте текущую папку:
```powershell
pwd
```

Должно показать: `C:\Users\user\Desktop\дз промпт 15.12`

Также можно проверить наличие файла:
```powershell
ls run_desktop.py
```

Если файл есть - вы в правильной папке!

### Шаг 3: Запустите приложение

```powershell
py run_desktop.py
```

---

## 🔍 Альтернативный способ (из любой папки)

Если вы находитесь в любой другой папке, можно запустить так:

```powershell
py "C:\Users\user\Desktop\дз промпт 15.12\run_desktop.py"
```

---

## ⚠️ Убедитесь, что сервер работает

**ВАЖНО:** Desktop приложение подключается к API серверу на `localhost:8000`.

Убедитесь, что:
1. API сервер запущен в **другом** окне PowerShell
2. Сервер работает на http://localhost:8000
3. Вы видите сообщение типа "Uvicorn running on http://0.0.0.0:8000"

Если сервер не запущен - сначала запустите его:
```powershell
cd "C:\Users\user\Desktop\дз промпт 15.12"
py run.py
```

---

## 📋 Полная последовательность команд:

**Терминал 1 (для API сервера):**
```powershell
cd "C:\Users\user\Desktop\дз промпт 15.12"
py run.py
```
(Оставьте это окно открытым)

**Терминал 2 (для Desktop приложения):**
```powershell
cd "C:\Users\user\Desktop\дз промпт 15.12"
py run_desktop.py
```

---

## ✅ После запуска

Должно открыться окно приложения "Competitor Analyzer" с вкладками:
- Image Analysis
- Text Analysis  
- Parsing
- History

