![Python](https://img.shields.io/badge/Python-3.9+-blue?logo=python&logoColor=white)
![AI](https://img.shields.io/badge/AI-Powered-purple)
![PyQt6](https://img.shields.io/badge/PyQt6-GUI-green)
![Flask](https://img.shields.io/badge/Flask-API-black?logo=flask)
![Status](https://img.shields.io/badge/Status-Active-success)
![License](https://img.shields.io/badge/License-MIT-yellow.svg)

# AI Competitor Market Analyzer

Персональный AI-анализатор рынка конкурентов с возможностью анализа изображений, текста и автоматического парсинга веб-сайтов конкурентов.

## Возможности

- **Анализ изображений**: Загрузка и анализ визуального контента конкурентов с оценкой дизайна (0-10) и потенциала анимации (0-10)
- **Анализ текста**: Анализ текстового контента с оценкой эффективности сообщений и рекомендациями
- **Парсинг сайтов**: Автоматический сбор данных с сайтов конкурентов с использованием Selenium
- **История анализа**: Сохранение всех анализов в локальной истории
- **Desktop приложение**: Удобное графическое приложение на PyQt6
- **REST API**: FastAPI бэкенд для интеграции с другими системами

## Установка

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/YOUR_USERNAME/competitor-analyzer.git
cd competitor-analyzer
```

*Замените `YOUR_USERNAME` на ваш GitHub username*

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

### 3. Настройте окружение (ДОБАВЬТЕ API КЛЮЧИ)

#### Способ 1: Автоматически (рекомендуется)
Запустите скрипт для создания файла `.env`:
```bash
python create_env.py
```
Скрипт попросит ввести ваш API ключ.

#### Способ 2: Вручную
Создайте файл `.env` в корне проекта и добавьте:
```env
OPENAI_API_KEY=your_openai_api_key_here
```

📖 **Подробная пошаговая инструкция:** см. файл `ИНСТРУКЦИЯ_API_КЛЮЧИ.md`

**Важно**: Никогда не загружайте файл `.env` в репозиторий! Он уже добавлен в `.gitignore`.

### 4. Настройте URLs конкурентов

Откройте `config.py` и укажите URLs сайтов конкурентов для парсинга:

```python
COMPETITOR_URLS = [
    "https://example-competitor1.com",
    "https://example-competitor2.com",
    # ... добавьте свои URLs
]
```

## Использование

### Запуск API сервера

```bash
uvicorn main:app --reload
```

API будет доступен по адресу: `http://localhost:8000`

### Тестирование эндпоинтов

#### Анализ изображения

```bash
curl -X POST "http://localhost:8000/analyzeimage" -F "file=@path/to/image.jpg"
```

#### Анализ текста

```bash
curl -X POST "http://localhost:8000/analyzetext" -H "Content-Type: application/json" -d '{"text": "Your text here"}'
```

#### Парсинг сайтов конкурентов

```bash
curl http://localhost:8000/parsedemo
```

#### Просмотр истории

```bash
curl http://localhost:8000/history
```

### Запуск Desktop приложения

```bash
python run_desktop.py
```

Приложение автоматически подключится к API серверу (убедитесь, что сервер запущен).

### Сборка исполняемого файла

Для создания standalone исполняемого файла:

```bash
python build.py
```

После сборки исполняемый файл будет находиться в `dist/competitionmonitor.exe`

## Структура проекта

```
.
├── main.py                 # FastAPI приложение
├── openaiservice.py        # Сервис для работы с OpenAI API
├── parsingservice.py       # Сервис для парсинга сайтов (Selenium)
├── config.py               # Конфигурация приложения
├── run_desktop.py          # Точка входа для desktop приложения
├── build.py                # Скрипт сборки исполняемого файла
├── gui/
│   └── main_window.py      # Главное окно desktop приложения
├── history/                # Директория для хранения истории анализов
├── requirements.txt        # Зависимости проекта
├── .env                    # Конфиденциальные данные (не в репозитории)
└── README.md               # Документация
```

## API Эндпоинты

- `GET /` - Информация об API
- `POST /analyzeimage` - Анализ изображения
- `POST /analyzetext` - Анализ текста
- `GET /parsedemo` - Парсинг сайтов конкурентов
- `GET /history` - Получение истории анализов
- `GET /health` - Проверка здоровья API

## Формат ответа анализа

Анализ возвращает JSON с следующими полями:

```json
{
  "success": true,
  "analysis": {
    "design_score": 8,
    "animation_potential": 7,
    "strengths": [...],
    "weaknesses": [...],
    "recommendations": [...],
    "overall_impression": "...",
    "competitive_positioning": "..."
  }
}
```

## Технологии

- **FastAPI** - Современный веб-фреймворк для Python
- **OpenAI API** - AI для анализа контента
- **Selenium** - Автоматизация браузера для парсинга
- **PyQt6** - GUI фреймворк для desktop приложения
- **PyInstaller** - Создание исполняемых файлов

## Требования

- Python 3.8+
- Chrome/Chromium браузер (для Selenium)
- OpenAI API ключ или Proxy API

## Безопасность

- **Никогда не загружайте `.env` файл в репозиторий**
- Файл `.env` автоматически игнорируется через `.gitignore`
- Храните API ключи в безопасности
- Используйте переменные окружения в production

## Лицензия

MIT License

## Автор

Разработано для анализа конкурентного рынка

