# ✅ Сервер запущен! Следующие шаги

## 🎉 Поздравляем! Ваш API сервер работает!

---

## 📖 Интерактивная документация API

Откройте в браузере:
```
http://localhost:8000/docs
```

Здесь вы увидите Swagger UI - интерактивную документацию, где можно:
- Видеть все доступные эндпоинты
- Тестировать их прямо из браузера
- Загружать изображения для анализа
- Отправлять тексты для анализа

---

## 🖥️ Запуск Desktop приложения

1. Откройте **НОВОЕ** окно PowerShell (старое оставьте с запущенным сервером)

2. Перейдите в папку проекта:
   ```powershell
   cd "C:\Users\user\Desktop\дз промпт 15.12"
   ```

3. Запустите desktop приложение:
   ```powershell
   py run_desktop.py
   ```

4. Откроется окно приложения с вкладками:
   - **Image Analysis** - анализ изображений
   - **Text Analysis** - анализ текста
   - **Parsing** - парсинг сайтов конкурентов
   - **History** - история анализов

---

## 🧪 Тестирование эндпоинтов

### 1. Анализ изображения

**Через браузер (Swagger UI):**
- Откройте http://localhost:8000/docs
- Найдите `/analyzeimage`
- Нажмите "Try it out"
- Выберите файл изображения
- Нажмите "Execute"

**Через командную строку:**
```powershell
curl -X POST "http://localhost:8000/analyzeimage" -F "file=@путь/к/изображению.jpg"
```

### 2. Анализ текста

**Через браузер (Swagger UI):**
- Откройте http://localhost:8000/docs
- Найдите `/analyzetext`
- Нажмите "Try it out"
- Введите текст для анализа
- Нажмите "Execute"

**Через командную строку:**
```powershell
$body = @{text="Ваш текст для анализа"} | ConvertTo-Json
Invoke-RestMethod -Uri "http://localhost:8000/analyzetext" -Method POST -Body $body -ContentType "application/json"
```

### 3. Парсинг сайтов конкурентов

**Через браузер:**
- Откройте http://localhost:8000/parsedemo
- Или через Swagger UI найдите `/parsedemo`

**Через командную строку:**
```powershell
Invoke-RestMethod -Uri "http://localhost:8000/parsedemo" -Method GET
```

### 4. Просмотр истории

**Через браузер:**
- Откройте http://localhost:8000/history

---

## 📁 Где сохраняются результаты?

Все анализы сохраняются в папке `history/` в формате JSON.

Файлы называются:
- `analysis_YYYYMMDD_HHMMSS.json` - для анализов изображений и текста
- `parsing_results_YYYYMMDD_HHMMSS.json` - для результатов парсинга

---

## 🔧 Настройка URLs конкурентов

Перед использованием парсинга, откройте `config.py` и укажите реальные URLs:

```python
COMPETITOR_URLS = [
    "https://competitor1.com",
    "https://competitor2.com",
    # добавьте свои URLs
]
```

---

## ✅ Чек-лист готовности

- [x] Сервер запущен на http://localhost:8000
- [ ] Desktop приложение запущено (опционально)
- [ ] Протестированы эндпоинты
- [ ] Настроены URLs конкурентов в config.py
- [ ] API ключ добавлен в .env

---

## 🎯 Рекомендуемый порядок действий

1. **Сначала протестируйте через Swagger UI** (http://localhost:8000/docs)
   - Это самый простой способ проверить работу API
   
2. **Затем запустите Desktop приложение** (если нужен графический интерфейс)

3. **Настройте URLs конкурентов** в config.py перед парсингом

4. **Протестируйте на реальных данных** - загрузите изображения конкурентов или вставьте их тексты

---

**Удачи! 🚀**

